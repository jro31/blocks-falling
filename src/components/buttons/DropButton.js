import RoundButton from './RoundButton';

const DropButton = () => {
  return (
    <RoundButton>
      {/* TODO */}
      <img src='/icons/drop-arrow.svg' alt='-' />
    </RoundButton>
  );
};

export default DropButton;
